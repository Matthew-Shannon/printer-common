[gcode_macro _MACRO_VARS]
variable_homing_speed: 0.0 # 50.0
variable_travel_speed: 0.0 # 200.0
variable_z_hop_dist: 0.0 # 2.0
variable_z_hop_speed: 0.0 # 1200.0
variable_x_probe_offset: 0.0 # 35
variable_y_probe_offset: 0.0 # 0.0
variable_bed_mid: 0.0 # 117.5
variable_wipe_center_x: 237.5
variable_purge_bucket_x: 250.0
gcode: G4 P10
description: Centralized state configuration registry for variable scoping

[delayed_gcode INIT_MACRO_VARIABLES]
initial_duration: 1
gcode:
    {% set c = printer.configfile.settings.constants %}
    {action_respond_info("<*> INIT: Fetching dynamic Kalico constants for macro vars")}
    SET_GCODE_VARIABLE MACRO=_MACRO_VARS VARIABLE=bed_mid VALUE={c.bed_mid|float}
    SET_GCODE_VARIABLE MACRO=_MACRO_VARS VARIABLE=z_hop_dist VALUE={c.z_hop_dist|float}
    SET_GCODE_VARIABLE MACRO=_MACRO_VARS VARIABLE=x_probe_offset VALUE={c.x_probe_offset|float}
    SET_GCODE_VARIABLE MACRO=_MACRO_VARS VARIABLE=y_probe_offset VALUE={c.y_probe_offset|float}
    SET_GCODE_VARIABLE MACRO=_MACRO_VARS VARIABLE=homing_speed VALUE={c.homing_speed * 60.0|float}
    SET_GCODE_VARIABLE MACRO=_MACRO_VARS VARIABLE=travel_speed VALUE={c.travel_speed * 60.0|float}
    SET_GCODE_VARIABLE MACRO=_MACRO_VARS VARIABLE=z_hop_speed VALUE={c.z_max_speed|float * 60.0|float}
    {action_respond_info("<*> INIT Complete: bed_mid=%.1f, z_hop=%.1f, x_off=%.1f, y_off=%.1f, home_spd=%.1f, travel_spd=%.1f, z_hop_spd=%.1f" % (c.bed_mid|float, c.z_hop_dist|float, c.x_probe_offset|float, c.y_probe_offset|float, c.homing_speed|float * 60.0, c.travel_speed|float * 60.0, c.z_max_speed|float * 60.0))}
description: initalizez the variables

# -------------------------------------------------------------------------------------------
# START_PRINT
# -------------------------------------------------------------------------------------------
[gcode_macro START_PRINT]
gcode:
    {% set bed_temp = params.BED_TEMP|default(0)|float %}
    {% set e_temp = params.EXTRUDER_TEMP|default(0)|float %}
    {% set filament_type = params.FILAMENT_TYPE|default('')|string|upper %}
    {% set z_tap_adjust = printer['probe'].tap_adjust_z|float %}
    {% set filament_offset = {"PLA": 0.0,"PETG": -0.020,"TPU": 0.0}.get(filament_type, 0.0)|float %}
    #{% set buildplate_offset = {}: float}

    EXCLUDE_OBJECT_DEFINE
    SET_FILAMENT_SENSOR_ON
    {action_respond_info("<*> START_PRINT: Bed=%.1f, Extruder=%.1f, Z_TAP=%.3f, Offset=%+.3f, Filament=%s" % (bed_temp, e_temp, z_tap_adjust, filament_offset, filament_type))}
    
    G90 ; Absolute positioning
    PROBE_EDDY_NG_SET_TAP_ADJUST_Z VALUE={(z_tap_adjust + filament_offset)|float}

    M104 S{e_temp-50}
    M190 S{bed_temp}

    AUTO_Z_TILT
    NOZZLE_WIPE

    AUTO_CENTER_TOOLHEAD
    PROBE_EDDY_NG_TAP
    BED_MESH_CALIBRATE

    M109 S{e_temp}
    AUTO_PURGE
    AUTO_CENTER_TOOLHEAD
    WLED_ON STRIP=chamber PRESET=PRINTING

description: Start print sequence with dual-stage wiping and purging

# -------------------------------------------------------------------------------------------
# END_PRINT
# -------------------------------------------------------------------------------------------
[gcode_macro END_PRINT]
gcode:
    {% set vars = printer["gcode_macro _MACRO_VARS"] %}
    {% set x_pos = printer.toolhead.axis_minimum.x|float %}     
    {% set y_pos = (printer.toolhead.axis_maximum.y - 15.0)|float %}
    {action_respond_info("<*> Calling END_PRINT moving to %d,%d" % (x_pos,y_pos))}

    AUTO_RETRACT_TIP
    AUTO_LIFT_Z HOP=50
    G90
    G1 X{x_pos} Y{y_pos} F{vars.travel_speed} ; Park

    TURN_OFF_HEATERS
    M84 ; disable motors
    M107 ; turn off fan
    SET_FILAMENT_SENSOR_OFF
    WLED_ON STRIP=chamber PRESET=COMPLETE
    
    PROBE_EDDY_NG_SET_TAP_OFFSET VALUE=0
    BED_MESH_CLEAR
    UPDATE_DELAYED_GCODE ID=CLEAR_FILE DURATION=1
description: End a print, turn off heaters, and park the toolhead

# -------------------------------------------------------------------------------------------
# CANCEL_PRINT
# -------------------------------------------------------------------------------------------
[gcode_macro CANCEL_PRINT]
rename_existing: BASE_CANCEL_PRINT
gcode:
    {action_respond_info("<*> Calling CANCEL_PRINT")}
    CLEAR_PAUSE
    END_PRINT
    BASE_CANCEL_PRINT
    WLED_ON STRIP=chamber PRESET=CANCEL
description: Cancel a print, turn off heaters, and park the toolhead

# -------------------------------------------------------------------------------------------
# PAUSE
# -------------------------------------------------------------------------------------------
[gcode_macro PAUSE]
rename_existing: BASE_PAUSE
gcode:
    {% set vars = printer["gcode_macro _MACRO_VARS"] %}
    {% set x_pos = (printer.toolhead.axis_maximum.x / 2.0)|float %}                                        
    {% set y_pos = (printer.toolhead.axis_minimum.y + 5.0)|float %}            
    {% set e_temp = printer['extruder'].target|float %}    

    {% if printer['pause_resume'].is_paused %}
        {action_respond_info("Printer is already paused.")}
        {exit()}  # Optional: halts macro execution cleanly in Klipper
    {% endif %}

    WLED_ON STRIP=chamber PRESET=PAUSE

    SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=zhop VALUE={vars.z_hop_dist} ; set z hop variable for reference in resume macro
    SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=etemp VALUE={e_temp}         ; set hotend temp variable for reference in resume macro                           

    SET_FILAMENT_SENSOR_OFF
    
    SAVE_GCODE_STATE NAME=PAUSE                                           ; save current print position for resume
    BASE_PAUSE      

    AUTO_RETRACT_TIP
    AUTO_LIFT_Z
    G90
    G1 X{x_pos} Y{y_pos} F{vars.travel_speed}                             ; park toolhead at front center
    SAVE_GCODE_STATE NAME=PAUSEPARK                                       ; save parked position in case toolhead is moved during the pause (otherwise the return zhop can error)
    M104 S0                                                               ; turn off hotend
description: Pause a print, optionally raising Z and parking the toolhead

# -------------------------------------------------------------------------------------------
# RESUME
# -------------------------------------------------------------------------------------------
[gcode_macro RESUME]
rename_existing: BASE_RESUME
variable_zhop: 0
variable_etemp: 0
gcode:
    {% set vars = printer["gcode_macro _MACRO_VARS"] %}
    {% set macro = printer['gcode_macro RESUME'] %}
    {% set e = params.E|default(2.5)|float %}                         ; hotend prime amount (in mm)
    {% set zhop = macro.zhop|float %}
    {% set etemp = macro.etemp|float %}
    {action_respond_info("<*> Calling RESUME")}

    {% if not printer['pause_resume'].is_paused %}
        {action_respond_info("<*> Print is not paused. No action taken.")}
        {exit()}  # Optional: halts macro execution cleanly in Klipper
    {% endif %}

    {% if zhop == 0 %}{action_respond_info("<*> Z hop is 0. No action taken.")}{% endif %}
    {% if zhop > 0 %}{action_respond_info("<*> Resuming print with Z hop of %.1f" % (zhop))}{% endif %}
    {% if etemp > 0 %}{action_respond_info("<*> Resuming print with hotend temp of %.1f" % (etemp))}{% endif %}
    WLED_ON STRIP=chamber PRESET=RESUME
    SET_FILAMENT_SENSOR_ON

    {% if etemp > 0 %} M109 S{etemp} {% endif %}                ; wait for hotend to heat back up
    RESTORE_GCODE_STATE NAME=PAUSEPARK MOVE=1 MOVE_SPEED=200    ; go back to parked position in case toolhead was moved during pause (otherwise the return zhop can error)
    G91                                                         ; relative positioning
    M83                                                         ; relative extruder positioning
    G1 Z{zhop * -1} F{ vars.z_hop_speed}                        ; lower Z back down without priming (just in case we are testing the macro with cold hotend)
    AUTO_PURGE
    RESTORE_GCODE_STATE NAME=PAUSE MOVE=1 MOVE_SPEED=200        ; restore position
    WLED_ON STRIP=chamber PRESET=PRINTING
    BASE_RESUME

description: Resume a print after a pause, optionally restoring position & temperatures

# -------------------------------------------------------------------------------------------
# M109
# -------------------------------------------------------------------------------------------
[gcode_macro M109]
rename_existing: M99109
gcode:
    {% set s = params.S|default(0)|float %}
    {action_respond_info("<*> Heating Extruder to: %.1f" % (s))}
    WLED_ON STRIP=chamber PRESET=HEATING
    M104 {% for p in params %}{p}{params[p]} {% endfor %}  ; Set hotend temp using safe token evaluation
    {% if s != 0 %} TEMPERATURE_WAIT SENSOR=extruder MINIMUM={s} {% endif %} ; Wait for hotend temp
    WLED_ON STRIP=chamber PRESET=IDLE
description: Heat the extruder to a specified temperature and wait until it reaches that temperature

# -------------------------------------------------------------------------------------------
# M190
# -------------------------------------------------------------------------------------------
[gcode_macro M190]
rename_existing: M99190
gcode:
    {% set s = params.S|default(0)|float %}
    {action_respond_info("<*> Heating Bed to: %.1f" % (s))}
    WLED_ON STRIP=chamber PRESET=HEATING
    M140 {% for p in params %}{p}{params[p]} {% endfor %}   ; Set bed temp using safe token evaluation
    {% if s != 0 %} TEMPERATURE_WAIT SENSOR=heater_bed MINIMUM={s} {% endif %} ; Wait for bed temp (within 2 degree)
    WLED_ON STRIP=chamber PRESET=IDLE
description: Heat the bed to a specified temperature and wait until it reaches that temperature

# -------------------------------------------------------------------------------------------
# CLEAR_FILE
# -------------------------------------------------------------------------------------------
[delayed_gcode CLEAR_FILE]
gcode: SDCARD_RESET_FILE
description: clear sd file after print

# -------------------------------------------------------------------------------------------
# FILAMENT
# -------------------------------------------------------------------------------------------
[delayed_gcode DISABLE_FILAMENT_SENSOR_ON_STARTUP]
initial_duration: 1
gcode: SET_FILAMENT_SENSOR_OFF
description: Disable filament sensor on startup

[gcode_macro SET_FILAMENT_SENSOR_OFF]
gcode: SET_FILAMENT_SENSOR SENSOR=sp_sensor_runout ENABLE=0
description: Disable filament sensor

[gcode_macro SET_FILAMENT_SENSOR_ON]
gcode: SET_FILAMENT_SENSOR SENSOR=sp_sensor_runout ENABLE=1
description: Enable filament sensor

[gcode_macro M600]
gcode: PAUSE
description: Filament change

# -------------------------------------------------------------------------------------------
# MMU
# -------------------------------------------------------------------------------------------

[gcode_macro _SP_PRINT_START]
gcode: {action_respond_info("<*> Calling _SP_PRINT_START")}
description: default no-op override

[gcode_macro _SP_PRINT_END]
gcode: {action_respond_info("<*> Calling _SP_PRINT_END")}
description: default no-op override

[gcode_macro _SP_SET_PURGE]
gcode: {action_respond_info("<*> Calling _SP_SET_PURGE")}
description: default no-op override

# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
# WLED (ENCLOSURE STRIP)
# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~================~~~~~~~

[delayed_gcode INIT_WLED]
initial_duration: 10
gcode: WLED_ON STRIP=chamber PRESET=Idle
description: initalize the lights

[gcode_macro WLED_ON]
gcode:
  {% set strip = params.STRIP|string %}
  {% set preset = params.PRESET|default("NA")|string|upper %}
  {% set preset_index = {"IDLE":1,"HEATING":2,"PRINTING":3,"PAUSE":4,"CANCEL":5,"RESUME":6,"HOMING":7,"COMPLETE":8}.get(preset, 0)|int %}
  {action_respond_info("<*> Calling WLED_ON STRIP=%s PRESET=%s INDEX=%i" % (strip,preset,preset_index))}
  {action_call_remote_method("set_wled_state",strip=strip,state=True,preset=preset_index)}
description: Turn WLED strip on using optional preset and resets led colors

[gcode_macro WLED_OFF]
gcode:
  {% set strip = params.STRIP|string %}
  {action_respond_info("<*> Calling WLED_ON STRIP=%s" % (strip))}
  {action_call_remote_method("set_wled_state",strip=strip,state=False)}
description: Turn WLED strip off

# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
# AUTO FUNCTIONS
# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

[gcode_macro AUTO_HOME]
gcode:
    {% set vars = printer["gcode_macro _MACRO_VARS"] %}
    {% set x_pos = (vars.bed_mid|float) - (vars.x_probe_offset|float) %}
    {% set y_pos = (vars.bed_mid|float) - (vars.y_probe_offset|float) %}
    {action_respond_info("<*> Calling AUTO_HOME with %.1f,%.1f" % (x_pos,y_pos))}

    WLED_ON STRIP=chamber PRESET=HOMING

    {% if "x" not in printer.toolhead.homed_axes %} G28 X {% endif %}
    {% if "y" not in printer.toolhead.homed_axes %} G28 Y {% endif %}
    G1 X{x_pos} Y{y_pos} F{vars.z_hop_speed}
    G28 Z

    G90
    G0 Z{vars.z_hop_dist} F{vars.z_hop_speed} ; for maximum sensor accuracy
    G4 S1                     
    M400                     
    PROBE_EDDY_NG_PROBE_STATIC HOME_Z=1 ; read the current exact height from sensor and home Z to it

    G0 Z5 F{vars.z_hop_speed} ; to 5mm as home

    WLED_ON STRIP=chamber PRESET=IDLE
description: Conditionally home X & Y and also home Z

[gcode_macro AUTO_LIFT_Z]
gcode:
    {% set vars = printer["gcode_macro _MACRO_VARS"] %}
    {% set z_hop_dist = params.HOP|default(vars.z_hop_dist)|float %} 
    {% set z_max = printer.toolhead.axis_maximum.z %}     ; maximum Z height
    {% set req = printer.gcode_move.position.z + z_hop_dist %} ; calculate requested Z height after hop

    G91 ; relative                                                               
    {% if req >= z_max %} ; if z max is exceeded, show message and set zhop value for resume to 0
        { action_respond_info("Pause zhop exceeds maximum Z height.") }                  
        SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=zhop VALUE=0
    {% else %}
         G1 Z{z_hop_dist} F{vars.z_hop_speed}  ; raise Z up by z hop amount using Kalico constants                  
    {% endif %}
    G90 ; absolute                                                            
description: Raise Z by a specified amount, ensuring it does not exceed the maximum Z height

[gcode_macro AUTO_CALIBRATE]
gcode:
    {% set bed_temp = params.BED_TEMP|default(65.0)|float %}
    {action_respond_info("<*> Calling AUTO_CALIBRATE with bed_temp=%.1f" % (req))}
    M84
    M190 S{bed_temp}
    AUTO_Z_TILT
    #AUTO_X_TWIST
    #AUTO_Y_TWIST
    SCREWS_TILT_CALCULATE
description: Auto calibrate the bed mesh and screws tilt

[gcode_macro AUTO_Z_TILT]
gcode:
    {action_respond_info("<*> Calling AUTO_Z_TILT")}
    G28
    Z_TILT_ADJUST
    G28 Z
description: Home and adjust Z-tilt

[gcode_macro AUTO_TAP_CHECK]
gcode:
    {% set bed_temp = params.BED_TEMP|default(65)|float %}
    {% set tap_adjust = params.TAP_ADJUST|default(printer['probe'].tap_adjust_z)|float %}
    M190 S{bed_temp}
    AUTO_Z_TILT
    AUTO_CENTER_TOOLHEAD
    PROBE_EDDY_NG_SET_TAP_ADJUST_Z VALUE={tap_adjust}
    PROBE_EDDY_NG_TAP

[gcode_macro AUTO_EDDY_CALIBRATE]
gcode: 
    {% set vars = printer["gcode_macro _MACRO_VARS"] %}
    {action_respond_info("<*> Calling AUTO_EDDY_CALIBRATE")}

    G28 X Y
    G1 X{vars.bed_mid} Y{vars.bed_mid} F{vars.travel_speed}

    {% for i in range(5) %} 
    PROBE_EDDY_NG_STATUS
    {% endfor %}

    M190 S65 ; heat bed to 65C for calibration

    PROBE_EDDY_NG_SETUP
description: Calibrate the EDDY probe and setup for bed mesh calibration

[gcode_macro AUTO_PID_EXTRUDER]
gcode: 
    {% set req = params.TEMP|float %}
    {action_respond_info("<*> Calibrating Extruder PID for: %.1f" % (req))}
    PID_CALIBRATE HEATER=extruder TARGET={req}
description: Calibrate the PID for the extruder heater to a specified temperature

[gcode_macro AUTO_PID_BED]
gcode: 
    {% set req = params.TEMP|float %}
    {action_respond_info("<*> Calibrating Bed PID for: %.1f" % (req))}
    PID_CALIBRATE HEATER=heater_bed TARGET={req}
description: Calibrate the PID for the bed heater to a specified temperature

[gcode_macro AUTO_RETRACT_TIP]
gcode:
    {% set vars = printer["gcode_macro _MACRO_VARS"] %}
    {% set retract_dist = -6.0|float %}
    {% set retract_speed = 900.0|float %}
    {action_respond_info("<*> Calling AUTO_RETRACT_TIP | G1 E%.1f F%.1f" % (retract_dist, retract_speed))}

    {% if not printer.extruder.can_extrude %}
        {action_respond_info("<*> Skipping AUTO_RETRACT_TIP Extruder too cold to retract tip safely")}
        {exit()}
    {% endif %}

    G91 ; relative                                                               
    G1 E{retract_dist} F{retract_speed}
    G90 ; absolute       
description: Retract the tip of the extruder by 6mm

[gcode_macro NOZZLE_WIPE]
gcode:
    {% set vars = printer["gcode_macro _MACRO_VARS"] %}
    {% set wipe_speed = 4800.0|float %}
    {% set half_width = 2.5|float %}
    {action_respond_info("<*> Calling NOZZLE_WIPE at X=%.1f" % (vars.wipe_center_x))}

    SAVE_GCODE_STATE NAME=nozzle_wipe_state
    G90
    G1 X{vars.wipe_center_x} F{vars.travel_speed}
    {% for i in range(5) %}
        G1 X{vars.wipe_center_x - half_width} F{wipe_speed}
        G1 X{vars.wipe_center_x + half_width} F{wipe_speed}
    {% endfor %}
    RESTORE_GCODE_STATE NAME=nozzle_wipe_state MOVE=1
description: Wipe the nozzle by moving it back and forth over a specified area

[gcode_macro AUTO_PURGE]
gcode:
    {% set vars = printer["gcode_macro _MACRO_VARS"] %}
    {% set purge_amount = 36.0|float %}
    {% set purge_speed = 150.0|float %}
    {% set retract_amount = -0.75|float %}
    {% set retract_speed = 2100.0|float %}
    {action_respond_info("<*> Calling AUTO_PURGE at X=%.1f" % (vars.purge_bucket_x))}

    {% if not printer.extruder.can_extrude %}
        {action_respond_info("<*> Extruder too cold to purge safely. Skipped.")}
        {exit()}
    {% endif %}

    SAVE_GCODE_STATE NAME=purge_bucket_state

    G90
    G1 X{vars.purge_bucket_x} F{vars.travel_speed}
    
    SAVE_GCODE_STATE NAME=purge_state   ; Captures the extrusion state mode (M82/M83) before purging
    M83                                 ; Set extruder to relative mode (highly recommended for Klipper)
    G92 E0                              ; Reset extruder position to 0
    G1 E{purge_amount} F{purge_speed}
    G4 S2 ; wait for 2 seconds
    G1 E{retract_amount} F{retract_speed}
    RESTORE_GCODE_STATE NAME=purge_state ; Reverts extruder mode back safely to whatever the printer or slicer was running
    
    RESTORE_GCODE_STATE NAME=purge_bucket_state
    NOZZLE_WIPE
description: Purge into bucket with thermal delay and wipe

[gcode_macro AUTO_CENTER_TOOLHEAD]
gcode: 
    {% set vars = printer["gcode_macro _MACRO_VARS"] %}
    G90 ; Force absolute coordinate mapping
    G0 X{vars.bed_mid} Y{vars.bed_mid} F{vars.travel_speed}
description: move toolhead to center

# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
# REF
# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~================~~~~~~~

# =========================================================================================
# MACRO FEEDRATE & SPEED CATEGORIES REFERENCE
# =========================================================================================
# This file utilizes standardized feedrates (F values in mm/min) across all macros.
# Global XY travel scales dynamically based on values set inside [gcode_macro _MACRO_VARS]
#
# 1. GLOBAL FAST TRAVEL (XY Rapids)
#    - Dynamic Feedrate : F12000 (Calculated via: travel_speed * 60 -> 200 * 60)
#    - Used for         : Toolhead parking, pause positioning, rapid center moves, 
#                         and travel to the wipe/purge stations.
#
# 2. NOZZLE WIPING SPEED
#    - Static Feedrate  : F4800 (80 mm/s)
#    - Used for         : The short, rapid back-and-forth scrubbing strokes over the brush.
#
# 3. EXTRUSION & PURGING SPEEDS
#    - Purge Feedrate   : F150  (Slow volumetric push to safely clear the nozzle melt zone)
#    - Retract Feedrate : F900  (Standard tip retraction speed)
#    - Un-retract Speed : F2100 (Snappy move to minimize oozing/stringing post-purge)
#
# 4. Z-AXIS HOP SPEEDS
#    - Dynamic Feedrate : Inherited from Kalico [constants] z_max_speed * 60
#    - Used for         : Safe Z hops during PAUSE, RESUME, and AUTO_LIFT_Z sequences.
# =========================================================================================
